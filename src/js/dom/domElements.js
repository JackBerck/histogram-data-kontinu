const form = document.querySelector("form");
const dataInput = document.querySelector("form textarea");
const svg = document.querySelector(".graph");
const xLine = document.querySelector("#xGrid line");
const yLine = document.querySelector("#yGrid line");

export { form, dataInput, svg, xLine, yLine };

// to capture HTML elements - done maybe
